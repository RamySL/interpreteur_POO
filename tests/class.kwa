var point a,b,c;
//var int point; //erreur
class point {
  attribute int x;
  attribute int y;
}

main {
  a = new point;
  b = new point;
  a.x = 1;
  a.y = 2;
  b.x = 3;
  b.y = 4;
  //print(this.x + a.y + b.x + b.y); // erreur
  // doit afficher 0 (egalité physique)
  if(a==b) {
    print(1);
  }else{
    print(0);
  }
  print(a.x + a.y + b.x + b.y);  // afiche 10
}
